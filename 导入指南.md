# PostgreSQL 数据库导入指南

## 文件说明

- `job_data.dump` - PostgreSQL 自定义格式备份文件（推荐，约 124MB）
- `job_data.sql` - SQL 文本格式备份文件（约 341MB）

---

## 场景一：目标机器使用 Docker 安装的 PostgreSQL（推荐）

### 前提条件

1. 已安装 Docker Desktop for Windows
2. PostgreSQL 容器正在运行

### 步骤 1：确认容器名称

```cmd
docker ps
```

找到 postgres 容器的 NAMES，例如 `postgres` 或 `my-pg`。后续命令用 `容器名` 替换。

### 步骤 2：复制文件到容器内

```cmd
docker cp "C:\Users\你的用户名\Desktop\pgdata\job_data.dump" 容器名:/tmp/job_data.dump
```

如果你的数据文件就在本项目目录 `pgdata/` 下，可以直接用相对路径（容器名以 `jobdata-pg` 为例）：

```cmd
docker cp ".\pgdata\job_data.dump" jobdata-pg:/tmp/job_data.dump
```

### 步骤 3：清除旧数据并导入

**方式 A：使用 --clean 自动清理（推荐）**

pg_restore 的 `--clean` 参数会自动删除旧表再导入，无需手动清理：

```cmd
docker exec -i jobdata-pg pg_restore -U postgres -d job_data --clean --if-exists /tmp/job_data.dump
```

如果数据库不存在，先创建：

```cmd
docker exec -it jobdata-pg psql -U postgres -c "CREATE DATABASE job_data;"
docker exec -i jobdata-pg pg_restore -U postgres -d job_data --clean --if-exists /tmp/job_data.dump
```

**方式 B：删除数据库重新创建（彻底清除）**

```cmd
REM 删除旧数据库
docker exec -it jobdata-pg psql -U postgres -c "DROP DATABASE IF EXISTS job_data;"

REM 创建新数据库
docker exec -it jobdata-pg psql -U postgres -c "CREATE DATABASE job_data;"

REM 导入数据
docker exec -i jobdata-pg pg_restore -U postgres -d job_data /tmp/job_data.dump
```

导入完成后（或在导入前）建议启用 pgvector 扩展：

```cmd
docker exec -it jobdata-pg psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

### 完整示例（容器名为 jobdata-pg）：

**方式 A：--clean 自动清理（推荐）**

```cmd
REM 复制文件
docker cp "C:\Users\BRM\OneDrive\Desktop\pgdata\job_data.dump" jobdata-pg:/tmp/job_data.dump

REM 创建数据库（如不存在）
docker exec -it jobdata-pg psql -U jobdata-pg -c "CREATE DATABASE job_data;"

REM 清除旧数据并导入
docker exec -i jobdata-pg pg_restore -U jobdata-pg -d job_data --clean --if-exists /tmp/job_data.dump

REM 启用 pgvector 扩展
docker exec -it jobdata-pg psql -U jobdata-pg -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

**方式 B：删除数据库重新创建（彻底清除）**

```cmd
REM 复制文件
docker cp "C:\Users\BRM\OneDrive\Desktop\pgdata\job_data.dump" jobdata-pg:/tmp/job_data.dump

REM 删除旧数据库
docker exec -it jobdata-pg psql -U jobdata-pg -c "DROP DATABASE IF EXISTS job_data;"

REM 创建新数据库
docker exec -it jobdata-pg psql -U jobdata-pg -c "CREATE DATABASE job_data;"

REM 导入数据
docker exec -i jobdata-pg pg_restore -U jobdata-pg -d job_data /tmp/job_data.dump

REM 启用 pgvector 扩展
docker exec -it postgres psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

### 完整示例（本项目 `pgdata/` + 容器名为 jobdata-pg）

推荐使用 `job_data.dump`：

```cmd
REM 1) 复制 dump 到容器
docker cp ".\pgdata\job_data.dump" jobdata-pg:/tmp/job_data.dump

REM 2) 创建数据库（如不存在）
docker exec -it jobdata-pg psql -U postgres -c "CREATE DATABASE job_data;"

REM 3) 清除旧数据并导入
docker exec -i jobdata-pg pg_restore -U postgres -d job_data --clean --if-exists /tmp/job_data.dump

REM 4) 启用 pgvector 扩展
docker exec -it jobdata-pg psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"

REM 5) 验证表是否存在
docker exec -it jobdata-pg psql -U postgres -d job_data -c "\dt"
```

备选：使用 `job_data.sql`：

```cmd
docker cp ".\pgdata\job_data.sql" jobdata-pg:/tmp/job_data.sql
docker exec -it jobdata-pg psql -U postgres -c "DROP DATABASE IF EXISTS job_data;"
docker exec -it jobdata-pg psql -U postgres -c "CREATE DATABASE job_data;"
docker exec -i jobdata-pg psql -U postgres -d job_data -f /tmp/job_data.sql
docker exec -it jobdata-pg psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
docker exec -it jobdata-pg psql -U postgres -d job_data -c "\dt"
```

### 使用 SQL 文件导入（备选）

```cmd
REM 复制 SQL 文件
docker cp "C:\Users\BRM\OneDrive\Desktop\pgdata\job_data.sql" postgres:/tmp/job_data.sql

REM 删除旧数据库
docker exec -it postgres psql -U postgres -c "DROP DATABASE IF EXISTS job_data;"

REM 创建新数据库
docker exec -it postgres psql -U postgres -c "CREATE DATABASE job_data;"

REM 导入
docker exec -i postgres psql -U postgres -d job_data -f /tmp/job_data.sql
```

---

## 场景二：目标机器使用原生 PostgreSQL 安装

### 前提条件

1. 已安装 PostgreSQL（推荐版本 12+）
2. `pg_restore`、`psql` 等工具在系统 PATH 中

### 方法一：使用 pg_dump 格式文件（推荐）

#### 清除旧数据并导入

```cmd
set PGPASSWORD=你的密码
pg_restore -U postgres -d job_data --clean --if-exists "C:\Users\你的用户名\Desktop\pgdata\job_data.dump"
psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

如果数据库不存在，先创建：

```cmd
set PGPASSWORD=你的密码
psql -U postgres -c "CREATE DATABASE job_data;"
pg_restore -U postgres -d job_data --clean --if-exists "C:\Users\你的用户名\Desktop\pgdata\job_data.dump"
psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

#### 彻底清除：删除数据库重新创建

```cmd
set PGPASSWORD=你的密码
psql -U postgres -c "DROP DATABASE IF EXISTS job_data;"
psql -U postgres -c "CREATE DATABASE job_data;"
pg_restore -U postgres -d job_data "C:\Users\你的用户名\Desktop\pgdata\job_data.dump"
psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

### 方法二：使用 SQL 文本文件

```cmd
set PGPASSWORD=你的密码
psql -U postgres -c "DROP DATABASE IF EXISTS job_data;"
psql -U postgres -c "CREATE DATABASE job_data;"
psql -U postgres -d job_data -f "C:\Users\你的用户名\Desktop\pgdata\job_data.sql"
psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

### PowerShell 用户

```powershell
$env:PGPASSWORD = "你的密码"
pg_restore -U postgres -d job_data --clean --if-exists "C:\Users\你的用户名\Desktop\pgdata\job_data.dump"
psql -U postgres -d job_data -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

---

## 注意事项

1. **版本兼容性**：pg_restore 格式文件通常兼容更高版本的 PostgreSQL，但不兼容更低版本
2. **权限问题**：确保 PostgreSQL 用户有足够的权限创建数据库和表
3. **编码问题**：如果导入时出现编码错误，可在连接时指定编码：
   ```
   psql -U postgres -d job_data -f "file.sql" --encoding=UTF8
   ```
4. **大数据量**：如果数据库较大，导入可能需要较长时间，请耐心等待
5. **Docker 清理**：导入完成后可删除容器内临时文件：
   ```cmd
   docker exec -it 容器名 rm /tmp/job_data.dump
   ```

## 故障排除

### Docker 相关

**问题：docker exec 报错 "No such container"**
**解决**：确认容器名称，使用 `docker ps -a` 查看所有容器

**问题：pg_restore 报错 "command not found"**
**解决**：容器内可能没有 pg_restore，尝试使用 SQL 文件方式导入，或使用带 pg_restore 的官方镜像

### 通用问题

**问题：pg_restore 报错 "could not open input file"**
**解决**：检查文件路径是否正确，路径中不要有中文或特殊字符

**问题：报错 "role 'postgres' does not exist"**
**解决**：用户名可能不是 postgres，请使用实际的 PostgreSQL 超级用户名

**问题：报错 "database 'job_data' does not exist"**
**解决**：需要先创建目标数据库，参考上面的"步骤 1"

**问题：连接被拒绝**
**解决**：检查 PostgreSQL 服务是否已启动，端口是否正确（默认 5432）
